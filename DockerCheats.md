##### run an image
```shell
docker run -p [port]:[port] [image]
```

##### build an image
```shell
docker build .
```

##### stop a container
```shell
docker stop [name]
```
##### list containers
```shell
docker ps
```
